const PREFIX="minerva.cart.";const normalize=items=>Array.isArray(items)?items.filter(item=>typeof item?.productId==="string"&&Number(item.quantity)>0).map(item=>({productId:item.productId,quantity:Number(item.quantity),displaySnapshot:{sku:String(item.displaySnapshot?.sku??""),productName:String(item.displaySnapshot?.productName??"")}})):[];
export function cartFor(username){const key=`${PREFIX}${username}`,read=()=>{try{return normalize(JSON.parse(localStorage.getItem(key)))}catch{return[]}},write=items=>{const safe=normalize(items);localStorage.setItem(key,JSON.stringify(safe));return safe};return{items:read,add(product,quantity=1){const items=read(),found=items.find(item=>item.productId===product.productId);if(found)found.quantity+=Number(quantity);else items.push({productId:product.productId,quantity:Number(quantity),displaySnapshot:{sku:product.sku,productName:product.productName}});return write(items)},set(productId,quantity){return write(read().map(item=>item.productId===productId?{...item,quantity:Number(quantity)}:item).filter(item=>item.quantity>0))},remove(productId){return write(read().filter(item=>item.productId!==productId))},clear(){localStorage.removeItem(key)}}}
