import{request}from"../../core/api.js";import{el}from"../../core/dom.js";import{money,dateTime,statusLabel}from"../../core/format.js";import{customerShell}from"../../ui/shells.js";import{heading,table,badge,loadingState,errorState}from"../../ui/components.js";
const tone=status=>status==="ENTREGADO"?"success":status==="CANCELADO"?"error":status==="PENDIENTE"?"warning":"info";
export function renderOrders({session}){const root=el("div",{className:"mx-auto grid max-w-7xl gap-6"},[heading("Mis pedidos","Consultá el estado y detalle de tus compras."),loadingState("Cargando pedidos…")]);customerShell(session,root);load();async function load(){try{const orders=await request("/api/v1/customer/orders",{token:session.accessToken});root.lastElementChild.replaceWith(table([{label:"Pedido",render:o=>el("a",{className:"font-mono font-bold text-secondary hover:underline",text:o.orderId,attrs:{href:`#/customer/orders/${encodeURIComponent(o.orderId)}`}})},{label:"Fecha",render:o=>el("span",{text:dateTime(o.registrationDate)})},{label:"Estado",render:o=>badge(statusLabel(o.status),tone(o.status))},{label:"Total",render:o=>el("b",{text:money(o.total)})}],orders,{empty:"Todavía no registraste pedidos."}))}catch(error){root.lastElementChild.replaceWith(errorState(error.message,load))}}}
