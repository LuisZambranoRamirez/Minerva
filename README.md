# Lenguajes_Programacion-Frontend

-- usa pgaudit
-- los id repiten codigo para mantener la integridad de los id en caso de cambios en el futuro
-- el servidor no debe modificar los datos, debe rechzarlos y pedir que se les envie de manera correcta
-- el campo de que usario fue el que registro cierta entidad solo crear en caso sea muy importante saber quien lo hizo (porque toda accion se registra en el sistema, pero se trata de evitar hacer consultas a la tabla audit)
